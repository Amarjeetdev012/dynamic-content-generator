<!DOCTYPE html>
<html>
<head>
	<title>Tag Input</title>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@yaireo/tagify/dist/tagify.css">
	<style>
		.tagify__input {
			border: none;
			outline: none;
			font-size: 16px;
			padding: 10px;
			border-radius: 5px;
			box-shadow: 0 0 3px #ccc;
			width: 100%;
			box-sizing: border-box;
		}

		.tagify__tag {
			background-color: #007bff;
			color: #fff;
			padding: 3px 6px;
			border-radius: 5px;
			margin-right: 5px;
			font-size: 14px;
		}
	</style>
</head>
<body>
	<h1>Tag Input</h1>
	<p>Type some text in the input field below with @ it become tag and simple text  become text.</p>
	<input type="text" id="inputField" class="tagify__input">
	
	<script src="https://cdn.jsdelivr.net/npm/@yaireo/tagify"></script>
	<script>
		let inputField = document.getElementById("inputField");
		let tagify = new Tagify(inputField, {
			mode: "mix",
			delimiters: "{ }",
			dropdown: {
				enabled: 0
			},
			transformTag: function(tagData) {
				return {
					value: "{" + tagData.value + "}"
				};
			}
		});

		inputField.addEventListener("keydown", function(event) {
			if (event.key === "Enter" && !tagify.isTagging()) {
				event.preventDefault();
				tagify.addTags(inputField.value);
				inputField.value = "";
			}
		});
	</script>
</body>
</html>
